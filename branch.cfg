cfgs:
    default:
        branches: ['master']
        inherit:
            landscaper:
                jobs:
                    release:
                        traits:
                            release:
                                nextversion: 'bump_minor'
    hotfix:
        branches: ['hotfix-.*']
        inherit:
            landscaper:
                jobs:
                    release:
                        traits:
                            release:
                                nextversion: 'bump_patch'

    int-test:
        branches: ['int-test']
        inherit:
            landscaper:
                jobs:
                  integration:
                    steps:
                      test:
                        image: 'dind:19'
                        privilege_mode: privileged
                        execute:
                        - integration-test

