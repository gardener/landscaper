$schema: "https://json-schema.org/draft/2019-09/schema"

definitions:
  credentials:
    type: object
    properties:
      accessKeyID:
        type: string
      accessKeySecret:
        type: string