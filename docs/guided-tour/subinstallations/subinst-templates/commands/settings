# base url of the component repository
REPO_BASE_URL="eu.gcr.io/gardener-project/landscaper/examples"

# path to the kubeconfig of the resource cluster, i.e. the cluster on which installations, targets, etc. are created
RESOURCE_CLUSTER_KUBECONFIG_PATH="/Users/D040602/tmp/kubes/laasds-temp001-kubeconfig.yaml"

# path to the kubeconfig of the target cluster used in the target of the resource cluster
TARGET_CLUSTER_KUBECONFIG_PATH="/Users/D040602/tmp/kubes/hubforplay-d040602-11-kubeconfig.yaml"

# namespace for resources in the resource cluster
NAMESPACE="cu-example"
